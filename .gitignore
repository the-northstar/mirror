# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# The PWA plugin regenerates these on every dev run, so a tracked copy turns
# any `bun run dev` into a dirty tree that blocks the next pull.
dev-dist

# Secrets — the repo is submitted publicly for judging.
.env
.env.*
!.env.example
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Store owner catalogue (file-backed).
products.json

# Stores, their API keys and SDK-fed shelves. Contains secrets.
stores.json

# Capacitor copies the web build in here on every sync; android/.gitignore
# covers the rest of the native build output.
android/app/src/main/assets/public
*.apk

# Store order ledger (file-backed).
orders.json

# Store product photos (file-backed).
uploads/
